import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Carbon Autopilot - AI-Powered Sustainability Dashboard",
  description:
    "Track your carbon footprint, get AI-powered recommendations, and join a global community reducing emissions. Built with Google Gemini AI.",
  keywords: [
    "carbon footprint",
    "sustainability",
    "emissions tracking",
    "AI recommendations",
    "eco-friendly",
    "climate action",
  ],
  authors: [{ name: "Carbon Autopilot" }],
  viewport: {
    width: "device-width",
    initialScale: 1,
    maximumScale: 1,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="bg-background">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
      </body>
    </html>
  );
}
